# import User from './user.graphql'
# import Comment from './comment.graphql'

type Pin {
  _id: ID!
  createdAt: String!
  title: String!
  content: String!
  image: String
  latitude: Float!
  longitude: Float!
  author: User!
  comments: [Comment!]!
}
